[phases.build]
cmds = ["pnpm --filter @workspace/daddyx build"]

[start]
cmd = "node artifacts/daddyx/.next/standalone/artifacts/daddyx/server.js"

[variables]
NODE_ENV = "production"
NEXT_TELEMETRY_DISABLED = "1"
PORT = "3000"
HOSTNAME = "0.0.0.0"
